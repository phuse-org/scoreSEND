% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_compile_data.R
\name{get_compile_data}
\alias{get_compile_data}
\title{Build subject compile data (TK removal and optional terminal SETCDs)}
\usage{
get_compile_data(
  studyid = NULL,
  path_db = NULL,
  fake_study = FALSE,
  xpt_dir = NULL,
  terminal_setcds_only = TRUE
)
}
\arguments{
\item{studyid}{Optional when \code{xpt_dir} is set; required for SQLite. Study identifier.}

\item{path_db}{Optional when \code{xpt_dir} is set; path of database}

\item{fake_study}{optional, Boolean \cr
whether study generated by SENDsanitizer package. When \code{TRUE}, compile data includes all treatment arms (e.g. vehicle, HD, and any other dose levels present in DM).}

\item{xpt_dir}{Optional; path to a directory containing XPT files for one study (flat: xpt_dir/bw.xpt, dm.xpt, etc.).}

\item{terminal_setcds_only}{If \code{TRUE} (default), after TK cleaning, keep only subjects whose \code{SETCD} is in \code{treatment_group} from \code{\link{get_treatment_group}} (terminal-sacrifice cohorts). If \code{FALSE}, all non-TK DM subjects are kept (no SETCD filter; broader cohort). Ignored when \code{fake_study} is \code{TRUE}.}
}
\value{
dataframe
}
\description{
Build subject compile data (TK removal and optional terminal SETCDs)
}
\examples{
\dontrun{
get_compile_data(studyid='1234123', path_db='path/to/database.db')
get_compile_data(xpt_dir='/path/to/study_folder')
}
}
