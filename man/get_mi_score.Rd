% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_mi_score.R
\name{get_mi_score}
\alias{get_mi_score}
\title{get MI score for a given study}
\usage{
get_mi_score(
  studyid = NULL,
  path_db = NULL,
  fake_study = FALSE,
  master_CompileData = NULL,
  score_in_list_format = FALSE,
  xpt_dir = NULL
)
}
\arguments{
\item{studyid}{Optional when \code{xpt_dir} is set; required for SQLite. Study identifier.}

\item{path_db}{Optional when \code{xpt_dir} is set; path of database}

\item{fake_study}{optional, Boolean \cr
whether study generated by SENDsanitizer package}

\item{master_CompileData}{optional; precomputed compile data}

\item{score_in_list_format}{optional; if \code{FALSE} (default), returns a long-format data frame with one score per subject per endpoint; if \code{TRUE}, returns the study-level list/data frame \code{mi_score_final_list_df} as before.}

\item{xpt_dir}{Optional; path to a directory containing XPT files for one study (flat: xpt_dir/mi.xpt, etc.).}
}
\value{
When \code{score_in_list_format} is \code{FALSE}, a data frame with columns \code{STUDYID}, \code{USUBJID}, \code{endpoint}, \code{score} (one row per subject per microscopical finding). When \code{TRUE}, the study-level summary (or \code{calculated_mi_score} in legacy code paths).
}
\description{
get MI score for a given study
}
\examples{
\dontrun{
get_mi_score(studyid='1234123', path_db='path/to/database.db')
get_mi_score(xpt_dir='/path/to/study_folder')
}
}
