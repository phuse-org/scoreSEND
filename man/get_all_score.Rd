% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_final_score.R
\name{get_all_score}
\alias{get_all_score}
\title{get all score}
\usage{
get_all_score(
  studyid = NULL,
  path_db = NULL,
  domain = c("lb", "mi", "bw"),
  fake_study = FALSE,
  score_in_list_format = FALSE,
  xpt_dir = NULL
)
}
\arguments{
\item{studyid}{Optional when \code{xpt_dir} is set; required for SQLite. Study identifier.}

\item{path_db}{Optional when \code{xpt_dir} is set; path of database}

\item{domain}{character or vector; domain name(s) to score (e.g. "lb", "mi", "bw").}

\item{fake_study}{optional, Boolean; whether study generated by SENDsanitizer package}

\item{score_in_list_format}{optional; if \code{FALSE} (default), each domain result is long format (same as \code{get_lb_score} / \code{get_mi_score} / \code{get_bw_score}); if \code{TRUE}, wide format per domain. Passed through to each scorer.}

\item{xpt_dir}{Optional; path to a directory containing XPT files for one study (flat: xpt_dir/bw.xpt, dm.xpt, etc.).}
}
\value{
List with \code{studyid_res} and one element per requested \code{domain} (\code{lb}, \code{mi}, \code{bw}): each is long or wide according to \code{score_in_list_format}.
}
\description{
get all score
}
\examples{
\dontrun{
get_all_score(studyid='1234123', path_db='path/to/database.db', domain=c('lb'))
get_all_score(xpt_dir='/path/to/study_folder', domain=c('lb'))
}
}
