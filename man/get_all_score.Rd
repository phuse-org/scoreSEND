% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_final_score.R
\name{get_all_score}
\alias{get_all_score}
\title{get all score}
\usage{
get_all_score(
  studyid = NULL,
  path_db = NULL,
  domain = c("lb", "mi", "bw"),
  fake_study = FALSE,
  xpt_dir = NULL
)
}
\arguments{
\item{studyid}{Optional when \code{xpt_dir} is set; required for SQLite. Study identifier.}

\item{path_db}{Optional when \code{xpt_dir} is set; path of database.}

\item{domain}{character or vector; domain name(s) to score (e.g. \code{lb}, \code{mi}, \code{bw}).}

\item{fake_study}{optional, Boolean; whether study generated by SENDsanitizer package.}

\item{xpt_dir}{Optional; path to a directory containing XPT files for one study (flat: \code{xpt_dir/bw.xpt}, \code{dm.xpt}, etc.).}
}
\value{
list with scores per domain and \code{studyid_res} (study identifier or \code{basename(xpt_dir)} when using XPT).
}
\description{
Get scores for specified domains. Data from SQLite (\code{path_db} + \code{studyid}) or from a directory of XPT files (\code{xpt_dir}; no \code{studyid} or \code{appid} required).
}
\examples{
\dontrun{
get_all_score(studyid = '1234123', path_db = 'path/to/database.db', domain = c('lb'))
get_all_score(xpt_dir = '/path/to/study_folder', domain = c('lb'))
}
}
