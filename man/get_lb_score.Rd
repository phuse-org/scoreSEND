% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_lb_score.R
\name{get_lb_score}
\alias{get_lb_score}
\title{get LB score for a given study}
\usage{
get_lb_score(
  studyid = NULL,
  path_db = NULL,
  fake_study = FALSE,
  master_CompileData = NULL,
  score_in_list_format = FALSE,
  xpt_dir = NULL
)
}
\arguments{
\item{studyid}{Optional when \code{xpt_dir} is set; required for SQLite. Study identifier.}

\item{path_db}{Optional when \code{xpt_dir} is set; path of database}

\item{fake_study}{optional, Boolean \cr
whether study generated by SENDsanitizer package}

\item{master_CompileData}{optional; precomputed compile data}

\item{score_in_list_format}{optional; if \code{FALSE} (default), returns a long-format data frame with one score per subject per endpoint; if \code{TRUE}, returns the study-level table \code{master_lb_scores} as before.}

\item{xpt_dir}{Optional; path to a directory containing XPT files for one study (flat: xpt_dir/lb.xpt, etc.).}
}
\value{
When \code{score_in_list_format} is \code{FALSE}, a data frame with columns \code{STUDYID}, \code{USUBJID}, \code{endpoint}, \code{score} (one row per subject per liver test, e.g. SERUM | ALT). When \code{TRUE}, the study-level table with \code{avg_*_zscore} columns.
}
\description{
Uses a unified day column for LB: VISITDY, or LBNOMDY, or LBDY (in that order). LBNOMLBL is not required.
}
\examples{
\dontrun{
get_lb_score(studyid='1234123', path_db='path/to/database.db')
get_lb_score(xpt_dir='/path/to/study_folder')
}
}
